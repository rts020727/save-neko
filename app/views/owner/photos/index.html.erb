<div class="container">
  <div class="row d-flex justify-content-center">

    <div class="col-12 clearfix mb-3">
      <h1 class="text-center">- MY PHOTOS -</h1>
      <div class="float-right"><%= link_to '新規投稿する', new_owner_photo_path, class: 'btn btn-sm btn-secondary' %></div>
    </div>
    
    <% if @photos.present? %>
      <% @photos.each do |photo| %>
        <div class="col-md-4">
          <div class='card card-columns mb-4 shadow-sm'>
            <div class="bd-placeholder-img card-img-top">
              <%= image_tag photo.get_image(600,400), class: "img-fluid img" %>
            </div>
            <div class="card-body">
              <h4><%= photo.title %></h4>
              <p class="card-text text-truncate">
                <%= photo.content %>
              </p>
              <div class="d-flex justify-content-between align-items-center">
                <div class="btn-group">
                  <%= link_to '編集', edit_owner_photo_path(photo.id), class: "btn btn-sm btn-outline-secondary"%>
                  <%= link_to '削除', owner_photo_path(photo.id), method: :delete, data: {confirm: "本当に削除しますか？"}, class: 'btn btn-sm btn-outline-secondary' %>
                  <%= link_to '詳細', owner_photo_path(photo.id), class: 'btn btn-sm btn-outline-secondary' %>
                </div>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    <% else %>
      <h3 class="text-center">
        投稿した写真はありません。<br>
        在籍しているねこちゃんの写真や、<br>
        お店の写真を投稿して魅力を発信しませんか？
      </h3>
    <% end %>
  </div>
</div>