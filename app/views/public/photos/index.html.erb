<div class="container">
  <div class="row d-flex justify-content-center">
    <% @photos.each do |photo| %>
      <div class="col-md-4">
        <%= render "photo", photo: photo %>
      </div>
    <% end %>
  </div>
</div>